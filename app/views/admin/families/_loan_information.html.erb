<% if family.loan %>
  <%= button_to "Update Loan", edit_loan_path(family.loan) , method: :get, class: "btn btn-warning btn-xs" %>
<% else %>
  <%= button_to "Add Loan", new_loan_path, method: :get, class: "btn btn-info btn-xs active" %>
<% end %>
