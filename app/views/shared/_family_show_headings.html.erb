<div>
  <h1 class="family-heading col-sm-offset-1"><%= @family.nationality.name %> Family of <%= @family.num_people %></h1>

  <div class="tags">
    <% if @family.categories %>
      <% @family.categories.take(2).each do |category| %>
        <%= link_to category.name, category_path(category), class: "btn btn-inverse" %>
      <% end %>
    <% end %>
  </div>
</div>
<h4 class="col-sm-5 col-sm-offset-1 details arrival-date">Arrival Date:
  <% if @family.retired? %>
    <h4 id="family-arrived">Family Arrived</h4></h4>
  <% else %>
    <%= format_date @family.arrival_date%></h4>
    <h4 class="col-sm-5 col-sm-offset-1 details donate-by">Donate by: <%= format_date @family.donation_deadline %>
    </h4>
<% end %>
